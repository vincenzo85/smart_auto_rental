spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/smart_rental
    username: smart_user
    password: smart_pass

app:
  integrations:
    api-key: "integration-docker-key"
  payment-core:
    mode: http
    base-url: "http://payment-core:8080"
    charge-path: "/api/v1/core/payments/charge"
    api-key: ""
    connect-timeout-ms: 2000
    read-timeout-ms: 5000
